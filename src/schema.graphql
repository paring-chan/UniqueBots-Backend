type Mutation {
    login(code: String!): String
}

type User {
    id: String!
    username: String!
    discriminator: String!
    tag: String!
    avatarURL: String!
    admin: Boolean!
}

type Query {
    me: User
}
